<!DOCTYPE html>
<html>
  <head>
    <title>SistemaDeArtigos</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Roboto:wght@400&display=swap" rel="stylesheet">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <style>
      :root {
        --bs-primary: #20c997;
        --bs-primary-rgb: 32, 201, 151;
      }
      a {
        color: var(--bs-primary);
      }
    </style>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <%= link_to "Sistema De Artigos", root_path, class: "navbar-brand" %>

        <%# --- BOTÃO HAMBURGER (NO LUGAR CORRETO) --- %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <%# DIV COLAPSÁVEL COM O ID CORRETO %>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if user_signed_in? && !current_user.admin? %>
              <li class="nav-item">
                <%= link_to "Meus Artigos", meus_artigos_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Enviar Novo Artigo", new_article_path, class: "nav-link" %>
              </li>
            <% elsif user_signed_in? && current_user.admin? %>
              <li class="nav-item">
                <%= link_to "Artigos Pendentes", admin_dashboard_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Gerenciar Artigos", gerenciar_artigos_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
          
          <ul class="navbar-nav">
             <% if user_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text me-3">
                  Olá, <%= current_user.email %>
                </span>
              </li>
              <li class="nav-item">
                <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-outline-light" %>
              </li>
            <% else %>
               <li class="nav-item">
                <%= link_to "Entrar", new_user_session_path, class: "btn btn-outline-light me-2" %>
               </li>
               <li class="nav-item">
                <%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-light" %>
               </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container mt-4 content-fade-in">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>

    <footer class="bg-dark text-white text-center p-4 mt-5">
      <div class="container">
        <p class="mb-0">© <%= Time.now.year %> Sistema de Artigos. Todos os direitos reservados.</p>
        <p>Desenvolvido com Ruby on Rails</p>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>